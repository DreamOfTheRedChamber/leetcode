package interview.cloudera;

/*
 * 1) Explain how you would implement a fixed capacity queue using an array for storage 2) What happens when "curl www.google.com" is done? 3) Given a root directory that contains jars in various subdirectories, print which jars contain a given class
 * 2) What happens when "curl www.google.com" is done? 
 * 3) Given a root directory that contains jars in various subdirectories, print which jars contain a given class
 * */

public class Question5 {

}
